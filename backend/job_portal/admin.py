from django.contrib import admin

# Job Portal admin configuration
# Core app admin is automatically registered in core/admin.py
# Users app admin is automatically registered in users/admin.py
# Orders app admin is automatically registered in orders/admin.py

# Register other inner apps admin here when they are created
# admin.site.register(SomeModel, SomeModelAdmin)
